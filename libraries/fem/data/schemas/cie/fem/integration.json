{
    "$id" : "/cie/fem/integration",
    "type" : "object",
    "description" : "Settings related to performing numerical quadrature.",
    "properties" : {
        "order" : {
            "type" : "integer",
            "minimum" : 1,
            "default" : 1,
            "description" : "Integration order in each direction."
        },
        "batch-size" : {
            "type" : "number",
            "minimum" : 1,
            "default" : 1e4,
            "description" : "Number of quadrature points at which to evaluate the integrand at once."
        },
        "device" : {
            "type" : "string",
            "enum" : ["host", "sycl"],
            "default" : "host",
            "description" : "Device to evaluate integrands on."
        }
    },
    "required" : [],
    "additionalProperties" : false
}
